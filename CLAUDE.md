Never use 'any' type in typescript.

Always scan code for reusable components instead of creating new ones.

Keep coding style consistent.

/web-old external directory is my legacy web dashboard which will be ported over to the electron app (similar functionality but may look different)

